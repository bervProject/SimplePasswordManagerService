language: node_js

services:
    - postgresql

node_js:
    - "14"

before_script:
    - psql -c 'create database spms;' -U postgres
    - yarn
    - yarn global add codecov

script:
    - yarn test
    - codecov
